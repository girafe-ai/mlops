[project]
name = "cats-and-dogs"
version = "0.1.0"
description = "Image classification challenge from Azira: Cats vs Dogs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.4",
    "torch>=2.9.0",
    "torchvision>=0.24.0",
]

[dependency-groups]
dev = [
    "fire>=0.7.1",
    "pre-commit>=4.3.0",
]
